<?php $this->headTitle('Latest'); ?>

<div class="hero-unit">
    <h1>Latest photos</h1>
</div>

<?php foreach($latest as $photo) : ?>
<div class="thumbnail">
    <h2><a href=""><?php echo $photo->getTitle();?></a></h2>
    <a href=""><?php echo $this->galleryThumbnail($photo, 500, 500, 'bounding'); ?></a>
    <div class="photo-info">Posted by <?php echo $photo->getCreatedByName(); ?> 
        on <?php echo $photo->getDateCreated()->format('jS F Y \a\t g:i a'); ?></div>
    <div class="description"><?php echo $this->escape($photo->getDescription());?></div>


    <hr>
</div>
<?php endforeach; ?>

